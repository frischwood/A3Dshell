# Core dependencies
numpy>=1.20.0
pandas>=1.3.0

# Geospatial processing
geopandas>=0.10.0
rasterio>=1.2.0
pyproj>=3.0.0
shapely>=1.8.0

# API and web requests
requests>=2.25.0

# Scientific computing
scipy>=1.7.0
matplotlib>=3.3.0

# Solar positioning (for horizon calculations)
pvlib>=0.9.0

# Mesh generation and 3D visualization
meshio>=5.0.0
pyvista>=0.32.0

# Geocoding (optional)
geopy>=2.2.0

# Template rendering
jinja2>=3.0.0

# GUI (Streamlit)
streamlit>=1.28.0

# Interactive mapping for ROI drawing
folium>=0.14.0
streamlit-folium>=0.15.0

# Configuration parsing (standard library, but listed for clarity)
# configparser (standard library)
